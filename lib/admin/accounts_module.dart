import 'package:flutter/material.dart';

class AdminModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Accounts Module Coming Soon!", style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
    );
  }
}
