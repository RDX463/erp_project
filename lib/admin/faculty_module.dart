import 'package:flutter/material.dart';

class FacultyModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Faculty Module Coming Soon!", style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
    );
  }
}
