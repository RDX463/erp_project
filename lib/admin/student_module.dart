import 'package:flutter/material.dart';

class StudentModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Student Module Coming Soon!", style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
    );
  }
}
