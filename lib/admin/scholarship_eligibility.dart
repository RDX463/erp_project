import 'package:flutter/material.dart';

class ScholarshipEligibilityPage extends StatelessWidget {
  const ScholarshipEligibilityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Scholarship Eligibility")),
      body: const Center(child: Text("Check Scholarship Eligibility")),
    );
  }
}
